.SUFFIXES:.c .o  
CC=gcc
SRCS=hello.c\
	pabc.c  
OBJS=$(SRCS:.c=.o) #use .o replace .c
EXEC=hello
start:$(OBJS) #only exec this target
	$(CC) $(OBJS) -o $(EXEC)
	@echo '----------ok--------' 
.c.o:
	$(CC) -o $@ -c $<
clean:
	rm -f $(OBJS)
